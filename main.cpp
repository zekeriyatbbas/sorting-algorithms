#include "tweet.h"

int main() 
{
	//std::vector<Tweet> db = readTweetsFromFile("tweets.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweetsSA.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweetsSD.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweetsNS.csv");

	//std::vector<Tweet> db = readTweetsFromFile("tweets5K.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweets10K.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweets20K.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweets30K.csv");
	//std::vector<Tweet> db = readTweetsFromFile("tweets50K.csv");
	
	//mergeSort(db,0,db.size(),"tweetID",true);
	//auto start = std::chrono::high_resolution_clock::now();
	//bubbleSort(db,"tweetID",true);
	//insertionSort(db,"tweetID",true);
	//mergeSort(db,0,db.size(),"tweetID",true);
	//binarySearch(db,1773335,"tweetID");
	//countAboveThreshold(db,"favoriteCount",250);
	//auto finish = std::chrono::high_resolution_clock::now();
	//std::cout<<finish - start<<std::endl;
	
}
